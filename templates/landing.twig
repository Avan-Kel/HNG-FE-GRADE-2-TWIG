{% extends "layout.twig" %}
{% block title %}Home — TicketApp{% endblock %}

{% block content %}
<!-- HERO SECTION -->
<section class="relative overflow-hidden bg-slate-50">
  <div class="w-full px-4 py-16">
    <div class="max-w-[1440px] mx-auto grid md:grid-cols-2 gap-8 items-center relative">
      <!-- Hero Text -->
      <div class="relative z-10">
        <h1 class="text-4xl font-extrabold">
          HNG TicketApp — Multi-framework Project
        </h1>
        <p class="mt-4 text-lg text-slate-700">
          Create, track, and resolve tickets across projects. Fast, accessible, consistent across frameworks.
        </p>
        <div class="mt-6 flex flex-wrap gap-3">
          <a href="/login.php" class="px-4 py-2 rounded bg-blue-600 text-white hover:bg-blue-700">
            Login
          </a>
          <a href="/signup.php" class="px-4 py-2 rounded border hover:bg-slate-100">
            Get Started
          </a>
        </div>
      </div>

      <!-- Hero Decorative Boxes & Circle -->
      <div class="relative z-0">
        <div class="absolute -right-12 -top-12 w-48 h-48 rounded-full bg-blue-100 opacity-60"></div>
        <div class="grid grid-cols-1 gap-4 mt-24 md:mt-0">
          <div class="p-6 bg-white rounded-lg shadow">Fast ticket creation</div>
          <div class="p-6 bg-white rounded-lg shadow">Real-time validation</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Hero Wavy SVG -->
  <svg viewBox="0 0 1440 120" class="w-full" preserveAspectRatio="none" aria-hidden="true">
    <path d="M0,48 C360,0 1080,96 1440,48 L1440 120 L0 120 Z" fill="#f1f5f9" />
  </svg>

  <!-- Additional Decorative Circle -->
  <div class="absolute -left-10 -bottom-10 w-32 h-32 rounded-full bg-blue-200 opacity-50 hidden md:block"></div>
</section>

<!-- FEATURE BLOCKS -->
<section class="max-w-[1440px] mx-auto mt-8 px-4 grid md:grid-cols-3 gap-6">
  <div class="p-6 bg-white rounded-lg shadow">Responsive & Accessible Design</div>
  <div class="p-6 bg-white rounded-lg shadow">Inline Validation & Error Feedback</div>
  <div class="p-6 bg-white rounded-lg shadow">Manage Tickets Anywhere</div>
</section>
{% endblock %}
